#include<stdio.h>



int main(){
    int TAM, l, c, cont=0;
    scanf("%d", &TAM);
    int m[TAM][TAM];
    for(l=0; l<TAM; l++){
        for(c=0; c<TAM; c++){
            scanf("%d", m[TAM][TAM]);
        }
    }
    for(l=0; l<TAM; l++){
        for(c=0; c<TAM; c++){
            if(m[l][c] == 1){
                     
            }
        }
    }
}